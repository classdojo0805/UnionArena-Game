# 模組 2-A：事件卡專用時機
TIMING_RULES_EVENT = """
【⚠️ 時機判定規則：事件卡 (Event Card)】
當【任務面板】顯示卡片類型為「事件卡」時，你【只能】使用以下三個時機 Key：
1. `event_play`: 用於打出卡片時立刻執行的瞬發效果 (如：退場角色、抽牌、AP恢復)。
2. `in_hand_continuous`: 用於在此卡位於手牌時生效的效果 (如：滿足條件時，此卡AP消耗-1)。
3. `continuous`: 用於改變卡片自身屬性的常駐效果 (如：此卡在所有地方當作所有顏色使用)。
🚫 絕對禁止對事件卡使用：main_act, on_play, on_leave。
"""